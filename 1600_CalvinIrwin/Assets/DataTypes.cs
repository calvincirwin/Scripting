using UnityEngine;
using System.Collections;

public class DataTypes : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Transform tran = transform;

		tran.position = new Vector3(2,6,0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
